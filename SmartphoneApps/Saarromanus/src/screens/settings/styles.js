import { StyleSheet } from 'react-native';

export default StyleSheet.create({
	// Main container
	container: {
		display: 'flex',
		width: '100%',
		height: '100%',
	},

	languageContainer: {
		flexBasis: '40%',
	},
});
